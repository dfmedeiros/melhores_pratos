- flash.each do |key, value|
  - unless value.nil?
    .alert{class: "alert-#{key}"}
      %button.close{ data: { dismiss: "alert" }} x
      - if value.is_a?(String)
        = value.html_safe
      - else
        = value
    - flash[key] = nil
